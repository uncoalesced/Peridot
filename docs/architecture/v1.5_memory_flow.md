# Peridot v1.5.2: Sovereign Kernel Architecture
# Engineered by uncoalesced.

The following diagram maps the exact thread lifecycle, hardware interrupts, and memory offloading protocols enforced by the v1.5.2 kernel.

```mermaid
graph TD
    subgraph API [Flask Orchestration]
        A[User Prompt Received] --> B{FSM Queue}
    end

    subgraph Watchdog [NVML Hardware Monitor Thread]
        W1[Poll RTX 5050 Registers] --> W2{VRAM > 7.5GB?}
        W2 -- Yes --> W3[Inject PANIC Event]
        W2 -- No --> W1
    end

    subgraph FSM [Kernel State Machine]
        B --> C[STATE: INTERRUPT_WAIT]
        C --> D[STATE: VRAM_PURGE]
        D --> E[SIGSTOP to Folding@home]
        E --> F{Watchdog Verify < 2GB}
        F -- Fail --> G[STATE: PANIC]
        F -- Success --> H[STATE: INFERENCE]
    end

    subgraph AetherRoute [LRU RAG Cache]
        H --> I{Vector in System RAM?}
        I -- Yes --> J[Zero-Latency Injection]
        I -- No --> K[Query NVMe SQLite DB]
        K --> L[Restore to RAM]
        L --> M{RAM Limit Reached?}
        M -- Yes --> N[Evict Oldest Vector to SSD]
        M -- No --> J
        N --> J
    end

    J --> O[llama.cpp Engine Execution]
    O --> P[STATE: COOLDOWN]
    P --> Q[STATE: IDLE / Resume FAH]
    W3 --> G

### > THE FINAL FUSION